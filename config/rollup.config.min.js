import babel from 'rollup-plugin-babel';
import { uglify } from "rollup-plugin-uglify";
import { banner } from "./banner";

export default {
  input: 'src/index.js',
  output: {
    file: 'dist/conversion.min.js',
    format: 'umd',
    name: 'conversion.js',
    banner
  },
  plugins: [
    babel({
      plugins: [
        'external-helpers'
      ]
    }),
    uglify({
      output: {
        preamble: banner
      }
    })
  ]
}